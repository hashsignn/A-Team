# Verified against a clean venv on Python 3.11.
#
# Deliberately small. The map, the charts and the globe are vendored under
# api/static/vendor/, so there is no front-end toolchain and nothing is
# fetched at runtime.
numpy>=2.0
pydantic>=2.9
PyYAML>=6.0
fastapi>=0.115
uvicorn>=0.32
pytest>=8.0

# The convening pack. Pure Python, no system libraries, and no font file to
# vendor: everything in the data folds to ISO-8859-1 except the arrow in lane
# names, which is transliterated.
fpdf2>=2.8
